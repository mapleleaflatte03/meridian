## 2024-05-27 - Disabled Button Styles
**Learning:** Found that `button[disabled]` elements in the application lack visually distinct styles, causing confusion about interactivity.
**Action:** Consistently implement `:disabled` styles (e.g., `opacity` and `cursor: not-allowed`) for all interactive elements to ensure state is clearly communicated to users.
